 node {
     stage('Clone repository') {
         echo '> Checking out the Git version '
         checkout scm
     }
     stage('Build') {
            steps {
                sh 'make' 
            }
  
 }
